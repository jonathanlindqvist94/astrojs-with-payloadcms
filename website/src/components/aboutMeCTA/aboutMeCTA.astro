---
import Text from "../text/text.astro";

const { title, description, image, link } = Astro.props;
const imageSrc = image?.url
  ? `${import.meta.env.PUBLIC_PAYLOAD_URL}${image.url}`
  : "";
---

<section aria-labelledby="about-me-title" class="flex w-full gap-10 mb-20">
  <div class="flex flex-col w-1/2 self-center">
    <Text id="about-me-title" as="h2" font="fontHeadings" weight="black">
      {title}
    </Text>
    <Text as="p" font="fontBody" weight="default">
      {description}
    </Text>
    <a
      href={link.reference.value.slug}
      class="px-6 py-3 rounded-lg bg-blue-700 cursor w-max text-white"
      >{link.reference.value.title}</a
    >
  </div>
  <img src={imageSrc} alt={image?.alt} class="w-1/2 h-auto object-cover" />
</section>
